using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "new 1HMelee", menuName = "Item/Weapon/OneHandMeleeWeapon")]
public class OneHandMeleeWeapon : WeaponItem
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
