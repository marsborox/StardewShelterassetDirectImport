using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "new 2HMelee", menuName = "Item/Weapon/TwoHandMeleeWeapon")]
public class TwoHandMeleeWeapon : WeaponItem
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
